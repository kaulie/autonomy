## Decision Cycle {{CYCLE}} — current values
Current values for the placeholders marked "{{DELTA_MARKER}}" above.

{{BRIEFING_NOTE}}```json
{{PAYLOAD}}
```

Reply with the AGENT_V2 Output Schema JSON for this cycle.
